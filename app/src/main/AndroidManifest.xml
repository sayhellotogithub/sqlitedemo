<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iblogstreet.sqlitedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.AddFeedReaderActivity" />
        <activity android:name=".view.UpdateFeedReaderActivity" />
        <activity android:name=".view.SearchFeedReaderActivity" />
        <activity android:name=".view.DeleteFeedReaderActivity" />
    </application>
</manifest>